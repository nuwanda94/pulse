# Benchmarks

This file is produced by `scripts/benchmark.py` (`make benchmark`).

Do not hand-edit throughput or latency figures. Until the harness has been run against a target environment, there are no published numbers.
